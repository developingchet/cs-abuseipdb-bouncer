# ==============================================================================
# CrowdSec AbuseIPDB Bouncer - Environment Configuration
# ==============================================================================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   chmod 600 .env
#
# Required variables must be set. Optional variables have sensible defaults.

# ------------------------------------------------------------------------------
# Required: CrowdSec LAPI Bouncer Key
# ------------------------------------------------------------------------------
# Generate with: docker exec crowdsec cscli bouncers add abuseipdb-reporter
# You will only see this key once — copy it immediately.
CROWDSEC_ABUSEIPDB_BOUNCER_KEY=

# ------------------------------------------------------------------------------
# Required: AbuseIPDB API Key
# ------------------------------------------------------------------------------
# Get your v2 API key from: https://www.abuseipdb.com/account/api
# Free tier: 1000 reports/day, 1000 checks/day
ABUSEIPDB_API_KEY=

# ------------------------------------------------------------------------------
# Optional: Daily Report Limit
# ------------------------------------------------------------------------------
# Maximum reports to send per UTC day. Resets at midnight UTC.
# Tiers: Free=1000, Webmaster=3000, Premium=50000
# Default: 1000
#ABUSEIPDB_DAILY_LIMIT=1000

# ------------------------------------------------------------------------------
# Optional: Pre-Check Before Reporting
# ------------------------------------------------------------------------------
# Call /check endpoint before /report to skip whitelisted IPs.
# Check and report quotas are independent (both 1000/day on free tier).
# Options: true, false
# Default: false
#ABUSEIPDB_PRECHECK=false

# ------------------------------------------------------------------------------
# Optional: Minimum Decision Duration Filter
# ------------------------------------------------------------------------------
# Skip decisions shorter than N seconds. Filters test/simulation events.
# Set to 300 to skip anything under 5 minutes.
# Set to 0 to disable the filter.
# Default: 0
#ABUSEIPDB_MIN_DURATION=0

# ------------------------------------------------------------------------------
# Optional: Log Level
# ------------------------------------------------------------------------------
# info  - Standard logging (reports, errors, warnings)
# debug - Verbose logging (includes skipped decisions, cooldowns, filters)
# Default: info
LOG_LEVEL=info
