linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - gosimple
    - unused

linters-settings:
  errcheck:
    check-type-assertions: true

issues:
  exclude-rules:
    # Test files may use blank identifiers for error returns intentionally.
    - path: _test\.go
      linters:
        - errcheck
